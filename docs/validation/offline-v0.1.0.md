# 离线版 0.1.0 验证记录

日期：2026-09-23。实现基于 `11a4c2c`，离线源码分支 `offline-v1`。

- `node --test --test-concurrency=2 tests/*.mjs`：336 项通过，0 失败。默认全并发与 Android 模拟器启动同时进行时，一项在线 socket 测试超时；该文件单独复测 10 项通过，全套降低测试并发后通过。没有通过放宽断言掩盖超时。
- 离线构建通过。Android Release / Debug 构建与 lintRelease 通过；lint 含固定横屏、旧系统忽略新属性等警告，没有构建错误。
- Android 发布 APK 签名验证通过，包名 `cn.duskrain.dustii.offline`、版本 `0.1.0`；保持固定发布证书。1593 个包内资源逐文件 SHA-256 与锁文件一致；编译 JS 与本次构建逐字节一致。
- Windows 包含 Node v22.23.2，下载包匹配 Node 官方 SHA-256。便携目录校验 1616 个文件全部通过。只监听本机回环地址。
- 手机布局浏览器验证：960×440 CSS 像素、DPR 2；阻止外部请求后仍可开局。观察到 605 次本地资源请求、0 次外部请求、0 个 JS 错误、1 个模拟 Worker；CacheStorage 为空。手枪弹药 20→19；暂停前后对局时间与显示倒计时一致。
- 布局编辑器实际拖动、缩放、保存成功；重新载入后保留。编辑器正常覆盖设置，关闭后回到设置。
- Windows 便携目录实测：1440×900 窗口、1440×810 渲染，9 名人机；W 移动坐标变化，手枪 20→19，1 个 Worker，无待确认射击。截图见 `docs/screenshots/offline-desktop-v1.png`。
- 45 秒浏览器观察：9 名人机、手机视口，保持连接。稳定阶段显示约 60 FPS、最近 300 帧 P95 为 16.8–22.2 ms；初始窗口包含暂停菜单的 15 FPS 帧，P95 66.8 ms。此记录是桌面 RTX 4060 上的手机视口模拟，不是 P60 真机跑分；没有记录逐帧最大耗时，因此不据此承诺无卡顿。

本次不宣称 P60 真机长时间稳定、所有地图角落与全部武器逐一验收。源码原有玩法测试通过不能代替实际手机测试。退出应用后的局内进度不保存，玩家设置可导出备份。
