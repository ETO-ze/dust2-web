# 离线版 0.2.0 验证记录

日期：2026-09-23。基于离线版 `8dd91e0`，源码分支 `offline-v1`。

## 自动检查与对局

- `node --test --test-concurrency=2 tests/*.mjs`：346 项通过，0 失败。新增用例覆盖轻松档、连败恢复、出生分散、连续枪声不重置回防、听觉范围、守备保留、回合清理、真实地图可站立与可达守点、道具绕路限制、携包者保护条件与精度查询无副作用。
- `node scripts/qa-bot-rework.mjs --output-dir=artifacts/offline-v0.2.0`：3 个固定种子，各 240 秒游戏时间、9 名人机，总计 12 分钟模拟。38 次已验证投掷均达到现有有效落点标准，预测与实际落点最大误差约 0.254 米。观察到推进、peek、回防、下包、拆包、捡枪等行为，玩家位置全部有限。原始记录见 [对局结果](offline-v0.2.0-soak.json)。
- 上述 3 组模拟的逻辑 tick P95 分别为 3.66、3.31、2.75 ms；最大值分别为 64.67、57.75、32.62 ms。仍存在单次尖峰，不能据此宣称没有卡顿。这是桌面 Node 的逻辑耗时，不是手机渲染帧率。
- `node scripts/qa-bot-holding.mjs`：4 种防守阵型各 90 秒模拟，共 6 分钟。20 名人机均有移动，最短累计移动 90 米；每组狙击手实际开镜 36–46 秒。原始记录见 [守点结果](offline-v0.2.0-holding.json)。
- 离线 Vite 构建、Android Release/Debug 构建与 lintRelease 通过。保留大 JS 块和 Android 平台兼容属性警告，没有构建错误。

## 手机界面与 Android

- Chrome 触屏模拟：960×440 CSS 像素、清晰档；新增三档选项，选择轻松后重载仍保留。设置面板与选择框在视口内，实际截图见 [难度设置](../screenshots/offline-difficulty-v02.png)。
- 阻止外部请求后可创建轻松档本机对局，房主通过界面将人机数设至 9，快照包含 10 名玩家；Worker 模拟、触控激活、CacheStorage 为空。进入对局观察约 110 秒，仍连接，无 WebGL 上下文丢失；页面 0 个 JavaScript 错误、1 个着色器警告。
- 浏览器最后 300 帧窗口 P95 为 22.3 ms，当时显示 58 FPS。未记录这段观察的逐帧最大值；不可用作 P60 跑分或长时间稳定保证。
- 正式签名 APK 已在 Android 11 模拟器从 0.1.0 覆盖安装到 0.2.0，版本代码 1→2，包名 `cn.duskrain.dustii.offline`，启动成功。
- 相同游戏构建的 Debug APK 使用既有测试证书覆盖升级；16 项已有设置及备份保持一致，包括亮度 110 和自定义触控布局。正式发布证书不变。首次 Debug 测试因新测试密钥不匹配而拒绝安装，随后沿用原测试密钥成功升级，没有卸载或清空数据。
- 原生 WebView 153、关闭 Wi-Fi/移动数据：三档难度可选，轻松档 9 名人机正常进入三维战场，触摸开火弹药 20→19，CacheStorage 为空。实际画面见 [Android 安装运行截图](../screenshots/offline-android-v02.png)。这是模拟器测试，不是 P60 真机测试。
- APK ZIP CRC 通过；1593 个锁定资源逐一匹配 SHA-256；包内页面、主程序、CSS 与 Worker 均与本次离线构建字节一致；v2 签名验证通过，证书 SHA-256 为 `6e3e79bcbdbbb20b2125182f4bfad2ab4a15ffc25d21cbcf0b2536e6ce9aa08d`。

## Windows 安装包

- 从最终 ZIP 新解压目录运行随包 Node 与校验脚本，1616 个文件的大小及 SHA-256 全部匹配，ZIP CRC 通过。运行时下载包也匹配 Node 官方校验值。
- 从该解压目录启动本机服务，仅监听 `127.0.0.1`；Chrome 中阻止外部请求后进入普通难度、9 名人机对局。W 键移动约 2.90 米，鼠标开火弹药 20→19，Worker 正常运行，无待确认射击。截图见 [Windows 实际运行](../screenshots/offline-windows-v02.png)。
- 此次 Windows 操作检查不是稳定帧率跑分；首次场景准备出现约 1.45 秒的 CPU/着色器提交样本，尚不能承诺首次载入没有停顿。
- Android 包 503,882,844 字节；Windows ZIP 380,766,163 字节。发布页提供两个包的 SHA-256 校验文件。

本版没有测得职业级实际爆头率或特定手机续航表现。测试覆盖的地图位置和对局种子有限，不承诺所有战术与角落均无问题；退出应用后的局内进度仍不保存。
